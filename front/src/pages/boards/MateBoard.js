import { useParams } from "react-router";


function MateBoard() {
    
    const {id} = useParams()
    return (
        <>
            <h3>여행 메이트 게시판</h3>
            

        </>
    );
}

export default MateBoard;